<div class="d-inline-flex gap-1">@if($registro->trashed()) @if($giPermissoes->permite('responsaveis.restaurar'))<form method="POST" action="{{ route('responsaveis.restore',$registro->id) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-success" title="Restaurar"><i class="bi bi-arrow-counterclockwise"></i></button></form>@endif @if($giPermissoes->permite('responsaveis.excluir_definitivamente'))<form method="POST" action="{{ route('responsaveis.force-destroy',$registro->id) }}" onsubmit="return confirm('A exclusão será definitiva. Deseja continuar?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger" title="Excluir definitivamente"><i class="bi bi-trash3-fill"></i></button></form>@endif @else @if($giPermissoes->permite('responsaveis.visualizar'))<a href="{{ route('responsaveis.show',$registro) }}" class="btn btn-sm btn-outline-dark" title="Visualizar"><i class="bi bi-eye-fill"></i></a>@endif @if($giPermissoes->permite('responsavel.editar'))<a href="{{ route('responsaveis.edit',$registro) }}" class="btn btn-sm btn-outline-primary" title="Editar"><i class="bi bi-pencil-fill"></i></a>@endif @if($giPermissoes->permite('responsaveis.excluir'))<form method="POST" action="{{ route('responsaveis.destroy',$registro) }}" onsubmit="return confirm('Deseja excluir este responsável?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" title="Excluir"><i class="bi bi-trash-fill"></i></button></form>@endif @endif</div>
