<div class="d-inline-flex gap-1">@if($registro->trashed()) @if($giPermissoes->permite($permissao.'.restaurar'))<form method="POST" action="{{ route($recurso.'.restore',$registro->id) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-success listagem-acao" title="Restaurar"><i class="bi bi-arrow-counterclockwise"></i></button></form>@endif @else @if($giPermissoes->permite($permissao.'.visualizar'))<a href="{{ route($recurso.'.show',$registro) }}" class="btn btn-sm btn-outline-dark listagem-acao"><i class="bi bi-eye-fill"></i></a>@endif @if($giPermissoes->permite($permissao.'.editar'))<a href="{{ route($recurso.'.edit',$registro) }}" class="btn btn-sm btn-outline-primary listagem-acao"><i class="bi bi-pencil-fill"></i></a><form method="POST" action="{{ route($recurso.'.toggle',$registro) }}">@csrf @method('PATCH')<button class="btn btn-sm {{ $registro->ativo?'btn-outline-warning':'btn-outline-success' }} listagem-acao"><i class="bi bi-toggle-on"></i></button></form>@endif @if($giPermissoes->permite($permissao.'.excluir'))<form method="POST" action="{{ route($recurso.'.destroy',$registro) }}" onsubmit="return confirm('Deseja excluir este registro?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger listagem-acao"><i class="bi bi-trash-fill"></i></button></form>@endif @endif</div>
